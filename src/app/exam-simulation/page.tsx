'use client';

export default function ExamSimulationPage() {
  return (
    <div className="max-w-3xl mx-auto animate-fade-in">
      <h1 className="mb-6">Exam Simulation</h1>
      <div className="rounded-lg p-8 text-center" style={{ background: 'var(--bg-card)', border: '1px solid var(--border)' }}>
        <p style={{ color: 'var(--text-muted)' }}>
          Full 3h 15m AFM exam simulation — Section A + Section B. Requires scraped content to activate.
        </p>
      </div>
    </div>
  );
}
